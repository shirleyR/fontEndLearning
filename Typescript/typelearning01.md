### 简单例子

```
let num: number;
const fetch = function (ur:stirng): PRomise {}

export const fetch = functionu(rl:string | object, params?:any, user?: User):Promise{

}

class Component{
    optionalArray?: Array<string>
    optionalBool?: boolean
    optionalFunc?: (foo:string, bar:number) => boolean
    requiredFunc: () => void   
}
```

### Typescript

- import 
- decorators
- async/await异步

```
let a: number
let b = true
let c: [string, number]
enum Color {Red, Green, Blue}
let d: { name: string } = { name: 'linkFly'}

let list_a: number[] = [1, 2, 3]
let list_b: Array<number> = [1, 2, 3]

（foo as string）

```